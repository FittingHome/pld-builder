/**
 * Return [baseStr] with the extension added if it wasn't there in the first place
 */
export function optionalAppend(baseStr: string, appendedStr: string): string;
export function stringIsUrl(s: string): boolean;
